<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layoutFragmentProductosAgregados"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="mx.galaxcom.lumiereapp.activities.pedidos.ListaProductosPedidoFragment">

    <TextView
        android:id="@+id/txtProductosAgregadosPedido"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginTop="20dp"
        android:text="@string/productosAgregadosPedido"
        android:textStyle="bold"
        android:textSize="27sp"/>

    <ListView
        android:id="@+id/lvProductosAgregadosPedido"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginTop="20dp"
        android:layout_below="@id/txtProductosAgregadosPedido">
    </ListView>

</RelativeLayout>
